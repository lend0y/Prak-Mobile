import 'package:flutter/material.dart';

import 'codelab/codelab_main.dart' as codelab;
import 'tugas/favorite_stateless.dart' as favStateless;
import 'tugas/favorite_stateful.dart' as favStateful;

void main() {
  // runApp(const codelab.MyAppCodelab());

  runApp(const favStateless.MyAppStateless());

  // runApp(const favStateful.MyAppStateful());
}
